services:
  k-agent:
    image: k-agent:snapshot
    build:
      context: .
      dockerfile: Dockerfile.snapshot
    container_name: k-agent-snapshot
    restart: unless-stopped
    ports:
      - "127.0.0.1:3001:3001"
